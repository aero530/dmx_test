[package]
name = "dmx_console"
version = "0.1.0"
edition = "2021"
description = "Host-side Ratatui console for the DMX interface (settings + live DMX monitor over USB)"
license = "MIT OR Apache-2.0"

[dependencies]
ratatui = "0.29"
crossterm = "0.28"
serialport = "4"
